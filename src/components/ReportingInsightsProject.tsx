import { ChartingPalettesProject } from './ChartingPalettesProject';

export function ReportingInsightsProject() {
  return (
    <ChartingPalettesProject
      heroTitle="Reporting Insights"
      heroTags={['#Reporting', '#agentic']}
      heroParagraphs={[
        'It started as technical experiment to use a chat interface to query report data to help Finance and HCM users answer their most critical business questions faster.',
      ]}
    />
  );
}
